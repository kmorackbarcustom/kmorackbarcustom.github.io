# KMO RACKBARCUSTOM — Daily 2026-09-08

## BK01 independent pilot
Gate 4 remains **PASS / LOCKED**. Work today is pre-Gate5 production safety and dark-deploy preparation only.

## Backup / restore evidence
- Created a local-only, ACL-restricted pre-Gate5 backup outside Git at `D:\AI-Workspace\backups\kmo-bk01\2026-09-08-pre-gate5`.
- A single SQL statement captured all 28 `public` tables in one PostgreSQL statement snapshot: 2,706 rows total.
- Critical snapshot counts: bookings 143; customers 80; orders 215; production allocations 21.
- Snapshot SHA-256: `e7beb1da1ff3bac1d6412a7259d6f951a49593b26234dcef5c1c35fe4f718c83`.
- Schema metadata SHA-256: `199f49f554fb12c2b16b03a273e07d5ef229c273b64a8537ca8677ea8ad29601`.
- Restore validation cast every backed-up row against the live production row type: 28/28 tables, 2,706/2,706 rows PASS.
- Emergency restore SQL is fail-closed (`ROLLBACK` by default); trigger suppression via transaction-local `session_replication_role=replica` was verified and rolled back.

## Data API / security preflight
- Current PostgREST exposed schemas are `public, graphql_public`; `local_service` is not exposed yet.
- `authenticator` has no manual `pgrst.db_schemas` override.
- Gate 5 must preserve `public` and `graphql_public` and add only `local_service` after the schema exists.
- Baseline anonymous column grants were minimized to exact consumer-selected fields; staff phone is no longer anonymously readable.
- Baseline SHA-256 after minimization: `a79867b65d91a3e7958a6d96b0d002f031f4c1c20cb5bff96683014689da60ea`.
- Static verifier PASS; PostgreSQL parser PASS (157 statements).

## Safety state
No Gate 5 DDL has been applied at this checkpoint. Existing KMO `public.*`, booking/order/production, LINE/Telegram, `booking.html`, and `bookingdashboard.html` remain untouched. Git changes are local only; **do not push** until Owner says so.

## Gate 5 database dark deploy

- Applied locked baseline to `KMO-Booking` ref `xfhpwxjywqgqefbncumm` as one transaction: PASS.
- `public.*` shape fingerprint before/after: `e5216d0a944aae965389b394b0414a91` / same: PASS.
- New schemas: `local_service`, `kmo_booking`, `kmo_bridge`: present.
- New-table RLS: 20/20 enabled; 0 disabled.
- `deposit-slips`: private, 5 MiB, JPEG/PNG/WebP only.
- Legacy booking/admin pages returned HTTP 200 after apply.
- No seed, mapping, backfill, route switch or customer cutover performed.
- PostgREST still exposes only `public, graphql_public`.
- Shared Developer profile correctly received HTTP 403 when attempting project API settings mutation; no config changed.
- Gate 5 DB dark deploy PASS; Data API exposure remains Owner/Admin-only pending action.
- Full evidence: `bk01-pilot/KMO_GATE5_DARK_DEPLOY_EVIDENCE.md`.