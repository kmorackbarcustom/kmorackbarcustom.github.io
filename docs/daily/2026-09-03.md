# Daily Project Log — 2026-09-03

**Project:** KMO RACKBARCUSTOM
**Agent/Owner:** Claude (Commander) / Owner
**Branch:** `main`
**HEAD:** `3034270`
**Working tree:** clean

## Done Today
- Pre-flight audit of production state before touching the Phase 5 rollout gate, as the work brief requires.
- Read live Supabase state directly (settings row, edge function version/entrypoint, function logs, session table) instead of trusting the docs.
- **Found two doc-vs-production discrepancies. No production change was made.**

## Discrepancy 1 — image rollout is already `all` in production
- Docs claim: `line_ai_image_rollout=owner_only`, owner 5-case live gate still pending.
- Production reality: `public.system_settings.line_ai_image_rollout = 'all'`.
- The row's `updated_at` reads `2026-08-29 06:14:25+00`, but `system_settings` has **no updated_at trigger**, so that timestamp is not proof of when the value flipped. There is no `audit_logs` entry for the change either.
- Effect: image AI is live for **all** LINE customers, not just the owner. The documented Phase 5 release gate was bypassed at some unknown point.

## Discrepancy 2 — Phase 4 is already deployed
- Docs claim: Phase 4 booking-payment grounding "PASS locally, deployment decision still open".
- Production reality: `line-webhook` is **v46**, not the documented v42. Its deployed entrypoint pins commit **`4b98aac`** (`feat(line-ai): notify staff on payment proof`) — i.e. every Phase 4 grounding commit plus the payment-proof staff notification is live.
- Deploy timestamp: `2026-08-30T11:23:37Z` (18:23 +07), after `4b98aac` was authored at 16:36 +07 the same day.

## Verified Healthy
- `line-webhook verify_jwt = false` — still correct after the v46 deploy.
- `internal-proxy` and vendored `line-oa-ai-module` untouched.
- Live behaviour spot check (2026-09-02 16:46 UTC, real customer `U21c0…602b`): grounded order-status reply, correct pending status and 4 ก.ย. 2569 due date, no hallucinated price/spec. No errors in the function log window.

## Resolved — owner confirmed
Both discrepancies are the owner's own doing, just never written down:
- The owner expanded `line_ai_image_rollout` to `all` and deployed v46 himself.
- He ran a live image test. Reported result: **slip/payment-proof images are recognised correctly, and the Telegram staff alert fires immediately** when one arrives. Path chosen: accept the current production state and correct the docs to match.

Docs reconciled this session:
- `PROJECT_CONTEXT.md` — header, Phase Status Ledger, Phase 5 table, Next Steps, Open Questions
- `PHASE5_IMPLEMENTATION_EVIDENCE.md` — version, rollout, phase status, Remaining Gate

## Still Open
- Phase 5 is **not** formally CLOSED: the payment-proof case passed live but nothing was recorded. Each Telegram payment-proof alert embeds the real LINE `messageId` — those messages are re-checkable raw evidence and should be pasted into the Phase 5 evidence doc.
- Remaining matrix cases with no result recorded: full-bike photo, close-up part, text-then-image session continuity, out-of-context image (must not invent service/product).
- Phase 4 needs no deployment decision anymore — it is already live in v46.

## Evidence
- `system_settings`: `line_ai_image_rollout='all'`, `line_ai_rollout='all'`, `line_ai_owner_uid='U0e4…1fa'`
- Edge function `line-webhook`: `version=46`, `verify_jwt=false`, `updated_at=1788089017335`, entrypoint imports `.../4b98aac/supabase/functions/line-webhook/index.ts`
- `pg_trigger` on `public.system_settings`: none (non-internal)
- `audit_logs` filtered on settings/image: 0 rows
