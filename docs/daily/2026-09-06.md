# Daily Project Log — 2026-09-06

**Project:** KMO RACKBARCUSTOM — BK01 Independent Pilot
**Agent/Owner:** ChatGPT / Owner
**Branch:** `pilot/bk01-independent-deployment`
**HEAD:** payment follow-up baseline `3e49375`
**Working tree:** payment/Stripe follow-up prepared on KMO pilot branch; legacy live booking untouched

## Done Today
- Audited canonical BK01 and KMO repo/runtime state before modification.
- Created `bk01-pilot/` from committed BK01 `82b297d` using `git archive`; canonical dirty DB files were excluded.
- Removed platform-admin, ticket runtime and Stripe billing runtime from the KMO copy.
- Added KMO Worker names, fail-closed deployment config, KMO shop redirect and configurable WSTERA attribution.
- Added upstream/sync, deployment and pilot finding documentation.
- Verified `npm audit` 0 vulnerabilities, tests 19/19 PASS, lint 0 errors, consumer/admin production builds PASS with TypeScript.

## Current State
- Source-ready local pilot only; not deployed and not cut over.
- Existing `booking.html` and `bookingdashboard.html` remain unchanged.

## Blocked
- KMO Supabase remote migration/RLS verification: CLI/connector lacks KMO authorization.
- KMO Cloudflare deploy/domain/rollback verification: Wrangler is not authenticated.

## Next
1. Authenticate KMO Supabase, record remote migration/RLS state and backup path.
2. Authenticate KMO Cloudflare, configure Worker env/domains and deploy preview/staging.
3. Run real browser/mobile end-to-end smoke before any production cutover.

## Payment / Stripe follow-up
- Confirmed KMO has no Stripe requirement for this pilot. Removed the remaining admin runtime query to `subscriptions`; entitlement remains DB-only for booking acceptance.
- Disabled KMO self-registration by redirecting `/register` to `/login`; this deployment is a design-partner instance, not public SaaS onboarding.
- Confirmed BK01 locally generates amount-specific PromptPay payloads and does not require promptpay.io.
- Confirmed legacy KMO booking uses a static QR image + fixed deposit + LINE slip flow.
- Important gate: the legacy value shown as PromptPay has a bank-account-like format and is not accepted by the BK01 PromptPay generator. A real KMO PromptPay alias must be confirmed before dynamic QR cutover.
- KMO LINE image flow can detect likely payment proof and notify Telegram, but remains notification evidence only, not settlement truth.
- Added `bk01-pilot/KMO_PAYMENT_FLOW.md` for booking deposits and the proposed separate future `payment_requests` capability for full job/order payments.
- Re-verified tests 17/17 PASS after removing the dead commercial/Stripe helper test, lint 0 errors, consumer/admin production builds PASS.

## Persistent KMO BK01 guardrails
- Added `bk01-pilot/AGENTS.md` as the mandatory rule set for humans/agents touching the pilot subtree.
- README now requires reading `AGENTS.md` first.
- Rules explicitly protect canonical BK01 from KMO mutations, define generic-defect upstream flow, KMO-only scope, Stripe-free runtime, PromptPay/payment boundaries, DB/deployment gates and legacy rollback protection.

## KMO BK01 Gate 1 database cutover checkpoint
- Owner completed Supabase CLI login on 2026-09-06.
- Added `bk01-pilot/KMO_DATABASE_CUTOVER_PLAN.md` locking the 9-gate parallel-schema/cutover strategy.
- Added `bk01-pilot/scripts/kmo-live-db-audit.sql`; verified it contains no DDL/DML command starts and is SELECT-only.
- Live KMO DB mutation remains prohibited until audit + schema-diff + rollback boundary are reviewed.
- Supabase CLI multi-profile setup on Mac completed for KMO, CraftBikeLab/shared access, and WSTERA Lab & Control.
- Linked this repository to KMO project `xfhpwxjywqgqefbncumm` and verified the linked ref.
- Gate 1 live database audit executed successfully via authenticated KMO CLI using SELECT-only SQL.
- Confirmed `public.bookings`, `public.orders`, `public.customers`, and `public.production_allocations` are populated; BK01 `local_service` and `kmo_bridge` are absent.
- Full audit captured schema/table/column/constraint/index/RLS/policy/trigger/function/storage/migration metadata plus row counts only; no customer rows or secrets were returned.
- Raw audit JSON is intentionally local-only and Git-ignored. Next gate is schema-contract lock / live-vs-BK01 diff; no production DDL performed.

## KMO BK01 Gate 2 schema contract checkpoint
- Gate 1 full live metadata audit completed through the linked KMO project; raw JSON evidence remains local-only and Git-ignored.
- Confirmed the live KMO booking model and BK01 booking model are semantically different and must not be merged in place.
- Added `bk01-pilot/KMO_SCHEMA_CONTRACT.md` locking ownership across `public.*`, `local_service.*` and `kmo_bridge.*`.
- Existing `public.bookings`, `public.customers`, `public.orders` and `public.production_allocations` remain protected KMO operational contracts.
- BK01 owns service/staff/time-slot/deposit booking in `local_service.*`; KMO vehicle/job/pickup/cart concerns remain outside canonical BK01 core.
- Stripe/platform-admin/ticket product surfaces remain excluded from the KMO pilot; compatibility objects are retained only where active app dependencies require them.
- Gate 2 verdict: PASS / LOCKED. Next is Gate 3 extension/bridge design; production DDL remains prohibited.

## KMO BK01 Gate 3 extension / bridge checkpoint
- Added `bk01-pilot/KMO_EXTENSION_DESIGN.md` and locked a four-domain model: `public`, `local_service`, `kmo_booking`, `kmo_bridge`.
- `kmo_booking.*` owns KMO-only vehicle/product/pickup/cart booking detail; `kmo_bridge.*` is mapping/reconciliation/cutover state only.
- Locked customer and booking bridges to support legacy-only, linked and BK01-only transition states without shared primary keys.
- Critical migration decision: do not synthesize legacy KMO bookings into BK01 time slots when service/staff/start/end data is missing.
- Existing legacy bookings stay in `public.bookings` through transition; genuine BK01 rows are created only from real BK01 booking data.
- Permanent dual-write is prohibited; any temporary compatibility projection must be idempotent, reconciled and have a removal gate.
- Gate 3 verdict: PASS / LOCKED. No production DDL executed. Gate 4 baseline + rollback package is next.
