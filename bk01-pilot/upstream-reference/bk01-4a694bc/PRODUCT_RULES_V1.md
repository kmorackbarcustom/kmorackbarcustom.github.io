> [!WARNING]
> **HISTORICAL / NON-AUTHORITATIVE AFTER BK-0 (2026-08-28).** This file is retained as implementation/business evidence only. Current product truth is governed by docs/DOCUMENTATION_INDEX.md, docs/PRODUCT_DECISIONS.md, and numbered docs/00_... through docs/10_.... Do not use old pricing, entitlement, LINE, storage, deployment, role, or marketing claims here as current requirements.

# 📋 PRODUCT RULES V1 (OFFICIAL SPECIFICATION)

> **สถานะ:** อนุมัติแล้ว (Approved by Khun Free - CEO)
> **ขอบเขต:** กติกาธุรกิจที่ schema, RPC และ API ทั้งหมดจะอ้างอิง
> **วันที่มีผล:** 2026-08-06

---

## 0. ข้อตกลงพื้นฐาน

| หัวข้อ | ค่าที่ล็อก |
| :--- | :--- |
| Timezone | `Asia/Bangkok` ทุกจุด |
| การเก็บเวลา | เวลาจริงเก็บเป็น `TIMESTAMPTZ` / เวลาทำการเก็บเป็น `TIME` (local) |
| Slot granularity | 15 นาที |
| จองล่วงหน้าได้เร็วสุด | 60 นาทีก่อนเวลานัด |
| จองล่วงหน้าได้ไกลสุด | 60 วัน |
| หน่วยเงิน | บาท เก็บเป็น `NUMERIC(10,2)` |
| Idempotency | ทุก write operation ที่สร้างของใหม่ต้องรับ idempotency key |
| ค่ากลาง buffer ของร้าน | 0 นาที (ให้ร้านค้าปรับตามบริการ) |
| เวลาทำการเริ่มต้น | 09:00 - 20:00 น. (พักเที่ยง 12:00 - 13:00 น.) |
| รูปแบบรหัสจองคิว | `BK-` + 6 ตัวอักษร/ตัวเลขสุ่มแบบตัดตัวสับสน (เช่น `BK-7K2M9Q`) ไม่ซ้ำทั้งระบบ |

---

## 1. สถานะการจอง (Booking Status)

แยกสองแกนออกจากกัน ห้ามปนใน column เดียว

### 1.1 `bookings.status`

| สถานะ | ความหมาย | กินสล็อตไหม | นับโควตาไหม |
| :--- | :--- | :---: | :---: |
| `hold` | ล็อกชั่วคราว รอชำระมัดจำ | ใช่ | ไม่ |
| `pending_review` | ส่งสลิปแล้ว รอร้านตรวจ | ใช่ | ไม่ |
| `confirmed` | ยืนยันคิวแล้ว | ใช่ | **ใช่** |
| `completed` | ให้บริการเสร็จแล้ว | — | นับไปแล้ว |
| `cancelled` | ยกเลิก (ลูกค้าหรือร้าน) | ไม่ | นับไปแล้ว |
| `no_show` | ถึงเวลาแล้วลูกค้าไม่มา | — | นับไปแล้ว |
| `expired` | hold หมดอายุ ไม่ได้ชำระ | ไม่ | ไม่ |

**นับโควตาตอน `confirmed` เท่านั้น** ไม่นับตอน `hold` — ไม่งั้นคนป่วนยิง hold รัวๆ กินโควตาร้านจนหมดได้

### 1.2 `bookings.deposit_status`

| สถานะ | ความหมาย |
| :--- | :--- |
| `not_required` | ร้านปิดการเก็บมัดจำ หรือบริการนั้นไม่เก็บ |
| `awaiting` | รอลูกค้าโอน |
| `submitted` | ลูกค้าอัปโหลดสลิปแล้ว |
| `verified` | ตรวจผ่าน (มือหรือออโต้) |
| `rejected` | ตรวจไม่ผ่าน |
| `refunded` | คืนเงินแล้ว (บันทึกย้อนหลัง ไม่มีระบบคืนอัตโนมัติใน V1) |

### 1.3 Transition ที่อนุญาต

```text
hold ──ส่งสลิป──> pending_review ──ร้านอนุมัติ──> confirmed ──> completed
 │                      │                             │
 │                      └──ร้านปฏิเสธ──> hold          ├──> cancelled
 │                         (ต่ออายุ hold ใหม่ 15 นาที)  └──> no_show
 │
 ├──หมดเวลา──> expired
 └──ลูกค้ายกเลิกเอง──> cancelled

ร้านไม่เก็บมัดจำ: hold ──> confirmed (ทันที)
```

Transition นอกเหนือจากนี้ต้องถูก reject ที่ชั้น database ไม่ใช่แค่ที่ UI
ทุกการเปลี่ยนสถานะบันทึกลง `booking_status_history` (ใคร เมื่อไหร่ จากอะไรเป็นอะไร เหตุผล)

### 1.4 ข้อความที่ลูกค้าเห็น (บังคับ)

| สถานะ | ข้อความ |
| :--- | :--- |
| `hold` | "จองไว้ให้แล้ว กรุณาโอนมัดจำภายใน X:XX นาที" |
| `pending_review` | "ได้รับสลิปแล้ว กำลังตรวจสอบการชำระเงิน" |
| `confirmed` | "ยืนยันคิวเรียบร้อย" |

**ห้ามแสดงคำว่า "อนุมัติแล้ว" หรือ "สำเร็จ" ก่อนสถานะเป็น `confirmed` เด็ดขาด**

---

## 2. Temporary Hold

| กติกา | ค่า |
| :--- | :--- |
| อายุ hold | 15 นาที นับจากสร้าง |
| ต่ออายุอัตโนมัติ | +5 นาที เมื่อเริ่มอัปโหลดสลิป (ต่อได้ครั้งเดียว) |
| อายุหลังร้านปฏิเสธสลิป | รีเซ็ตเป็น 15 นาทีใหม่ |
| จำนวน hold พร้อมกันต่อเบอร์โทร | 2 รายการ |
| การปล่อยคิว | job กวาดทุก 1 นาที + เช็ค `expires_at` ตอน query availability ด้วย |

การเช็คคิวว่างต้อง**กรอง hold ที่หมดอายุแล้วออกทันทีตอน query** ไม่รอ job เพราะ job อาจช้าได้

---

## 3. การคำนวณคิวว่าง (Availability)

### 3.1 หลักการ

คิวว่างคือช่วงเวลาที่ผ่านเงื่อนไข**ทุกข้อพร้อมกัน** ไม่ใช่การเช็คจุดเวลาเริ่มต้นอย่างเดียว

ช่วงที่ต้องตรวจคือ `[start, start + duration + buffer_after]`

```text
ช่วงเวลางาน
  ∩ เวลาทำการร้านวันนั้น
  ∩ เวลาทำงานของช่างวันนั้น
  − เวลาพักของช่าง
  − วันหยุดร้าน
  − วันลาช่าง
  − booking เดิมที่ยัง active (hold / pending_review / confirmed)
  − buffer ก่อนและหลังงานข้างเคียง
```

### 3.2 ลำดับความสำคัญของวันหยุด (สูงไปต่ำ)

1. วันหยุดร้าน (`shop_closures`) — ปิดทั้งร้าน ชนะทุกอย่าง
2. วันลาช่าง (`staff_time_off`) — ปิดเฉพาะช่างคนนั้น
3. วันหยุดประจำสัปดาห์ของช่าง (`staff_schedules.is_working_day = false`)
4. เวลาทำการร้าน
5. เวลาพักของช่าง

### 3.3 ระยะเวลาและ buffer

| ค่า | มาจากไหน |
| :--- | :--- |
| duration | `staff_services.custom_duration_minutes` ถ้ามี ไม่งั้นใช้ `services.duration_minutes` |
| buffer ก่อนงาน | `services.buffer_before_minutes` ถ้าเป็น NULL ใช้ค่ากลางของร้าน |
| buffer หลังงาน | `services.buffer_after_minutes` ถ้าเป็น NULL ใช้ค่ากลางของร้าน |
| ค่ากลางของร้าน | `shops.default_buffer_minutes` ค่าเริ่มต้น 0 |

buffer กำหนดที่ระดับบริการ เพราะล้างรถกับตัดผมต้องการเวลาเก็บงานต่างกันมาก

### 3.4 การเลือกช่างเมื่อลูกค้าเลือก "ใครก็ได้"

ต้องเป็นการเลือกจริง ไม่ใช่การสุ่ม และ**ต้องผูกช่างตั้งแต่ตอนสร้าง hold** ไม่ใช่ตอนยืนยัน (ไม่งั้นล็อกทรัพยากรไม่ได้)

เกณฑ์คัดกรอง:
1. ช่างต้องผูกกับบริการนั้นใน `staff_services` และ `is_active = true`
2. ต้องว่างตลอดช่วง `[start − buffer_before, end + buffer_after]`

เกณฑ์เลือกจากผู้ผ่านการคัดกรอง:
1. คนที่มีงานในวันนั้นน้อยที่สุด (กระจายงาน)
2. เสมอกัน → คนที่มีช่องว่างติดกับงานเดิมน้อยที่สุด (ลดเวลาว่างคั่น)
3. ยังเสมอ → เรียงตาม `staff_id` เพื่อให้ผลลัพธ์ deterministic ทดสอบซ้ำได้

### 3.5 ตารางเวลาที่ขาดหาย (Fail-Closed)

- ถ้าไม่มีแถว `staff_schedules` ของพนักงานสำหรับ `day_of_week` ที่ต้องการ ให้ถือว่าพนักงานคนนั้น **ไม่ว่าง**
- กฎนี้ใช้เหมือนกันทั้งกรณีลูกค้าระบุพนักงานและเลือก "ใครก็ได้"
- Backend RPC ต้องเป็นผู้บังคับใช้กฎสุดท้าย และ frontend ต้องปิดสล็อตให้ตรงกัน ห้ามตีความว่าแถวที่หายคือวันทำงานโดยปริยาย
- ร้านต้องตั้งตารางเวลาของพนักงานให้ครบก่อนพนักงานคนนั้นจะรับการจองได้ เป็นการเลือกแบบ fail-closed เพื่อไม่ให้รับคิวระหว่าง onboarding ที่ตั้งค่าไม่ครบ

---

## 4. มัดจำ (Deposit)

### 4.1 การคิดยอด

ลำดับ:
1. ถ้า `shops.require_deposit = false` → `deposit_status = not_required`, ข้ามไป `confirmed`
2. ถ้า `services.deposit_amount > 0` → ใช้ค่านั้น
3. ถ้าไม่ → ใช้ `shops.default_deposit_amount`
4. ถ้ายังเป็น 0 → `not_required`

ยอดมัดจำถูกคำนวณและตรึงไว้ที่ฝั่ง server ตอนสร้าง hold **ห้ามรับยอดจาก client** และเก็บสำเนา `service_price`, `duration`, `deposit_amount` ณ เวลาจองไว้ในแถว booking (ราคาที่ร้านแก้ทีหลังต้องไม่กระทบคิวที่จองไปแล้ว)

### 4.2 PromptPay QR

- payload สร้างที่ฝั่ง server ตามมาตรฐาน EMVCo พร้อมระบุจำนวนเงิน
- ห้ามสร้างที่ฝั่ง client และห้าม hardcode เลขบัญชี
- QR ผูกกับ booking หนึ่งรายการ หมดอายุพร้อม hold

### 4.3 กติกาสลิป (บังคับใช้ตั้งแต่โหมดตรวจมือ)

| กติกา | รายละเอียด |
| :--- | :--- |
| ไฟล์ที่รับ | jpg, png, pdf ขนาดไม่เกิน 5 MB |
| ที่เก็บ | private bucket เท่านั้น เข้าถึงผ่าน signed URL อายุ 5 นาที |
| กันสลิปซ้ำ | เก็บ `trans_ref` พร้อม unique index ทั้งระบบ ใบเดิมใช้ได้ครั้งเดียว |
| ยอดเงิน | ต้อง ≥ ยอดมัดจำที่ตรึงไว้ |
| เวลาโอน | ต้องอยู่ในช่วงไม่เกิน 24 ชม. ก่อนเวลาที่ส่งสลิป |
| จำนวนครั้งที่ส่งได้ | 3 ครั้งต่อ booking หลังจากนั้นล็อกให้ร้านจัดการเอง |

ในโหมดตรวจมือ ร้านเป็นคนกรอก/ยืนยัน `trans_ref` จากภาพ ระบบเช็คซ้ำให้
เมื่อต่อ API ตรวจอัตโนมัติในภายหลัง กติกาชุดนี้ไม่เปลี่ยน เปลี่ยนแค่ผู้กรอก

### 4.4 การคืนเงิน

V1 **ไม่มีระบบคืนเงินอัตโนมัติ** — PromptPay คืนเงินอัตโนมัติไม่ได้อยู่แล้ว
สถานะ `refunded` มีไว้ให้ร้านบันทึกย้อนหลังหลังโอนคืนเองเท่านั้น

---

## 5. ยกเลิก เลื่อนคิว และไม่มาตามนัด

| กติกา | ค่าเริ่มต้น | ร้านปรับได้ไหม |
| :--- | :--- | :---: |
| ลูกค้ายกเลิกเองได้ถึง | 24 ชม. ก่อนนัด | ได้ |
| มัดจำเมื่อลูกค้ายกเลิก | ไม่คืน | ได้ |
| ลูกค้าเลื่อนคิวเองได้ | 1 ครั้ง ถึง 12 ชม. ก่อนนัด | ได้ |
| มัดจำเมื่อเลื่อนคิว | ยกยอดเดิมไปคิวใหม่ | ไม่ |
| ร้านยกเลิกให้ | ได้ตลอด ต้องระบุเหตุผล | — |
| ทำเครื่องหมาย no-show ได้เมื่อ | ผ่านเวลานัดไป 15 นาที | ได้ |
| ผลของ no-show | `customers.no_show_count += 1` | — |
| ขึ้น blacklist อัตโนมัติ | no-show ครบ 3 ครั้งในร้านเดียวกัน | ได้ |

blacklist มีผลเฉพาะร้านนั้น ไม่ข้ามร้าน (คนละร้านคนละความสัมพันธ์กับลูกค้า)
การเลื่อนคิวคือการสร้าง hold ใหม่ + ยกเลิกอันเดิมใน transaction เดียว ไม่ใช่การแก้เวลาในแถวเดิม

---

## 6. โควตาและแพ็กเกจ

### 6.1 เส้นแบ่งแพ็กเกจ

| ฟีเจอร์ | Free Trial (14 วัน) | Basic 490 | Pro 990 |
| :--- | :---: | :---: | :---: |
| คิวจอง/เดือน | 50 | 100 | 500 |
| พนักงาน | 5 | 5 | 10 |
| Dashboard + ตารางช่าง + วันหยุด | ✅ | ✅ | ✅ |
| PromptPay QR + ตรวจสลิปมือ | ✅ | ✅ | ✅ |
| แจ้งเตือนร้านเมื่อคิวเข้า (LINE) | ✅ | ✅ | ✅ |
| ใบจอง + ยืนยันมัดจำ เข้า LINE ลูกค้า | ✅ | ✅ | ✅ |
| **เตือนลูกค้าก่อนนัด 24 ชม. / 1 ชม.** | ✅ | ❌ | ✅ |
| **เตือนซ้ำเมื่อยังไม่โอนมัดจำ** | ✅ | ❌ | ✅ |
| **ตรวจสลิปอัตโนมัติ** | 10 ครั้ง | ❌ | 100 ครั้ง/เดือน |

หลักการ: เส้นแบ่งอยู่ที่ **reminder** ไม่ใช่ "มี/ไม่มี LINE"

### 6.2 การนับโควตา

| กติกา | รายละเอียด |
| :--- | :--- |
| นับเมื่อ | booking เข้าสถานะ `confirmed` |
| ยกเลิกทีหลัง | ไม่คืนโควตา |
| รอบเดือน | นับจากวันสมัคร (billing anchor) ไม่ใช่วันที่ 1 |
| เมื่อโควตาหมด | ปิดรับจองใหม่ คิวเดิมทำงานต่อได้ปกติ + แจ้งร้านทันที |
| ลำดับการหัก | หักโควตาหลักก่อน หมดแล้วค่อยหัก top-up |
| top-up | เก็บเป็น ledger ทบข้ามเดือนได้ ไม่มีวันหมดอายุ (+100 คิว = 199 บาท / +100 ตรวจสลิป = 99 บาท) |

โควตาต้องถูกเช็คที่ชั้น database ใน transaction เดียวกับการ confirm ไม่ใช่เช็คที่ UI

### 6.3 กัน trial ซ้ำ

- 1 trial ต่อเบอร์โทรร้านที่ยืนยัน OTP แล้ว
- ร้านที่ trial หมดและไม่สมัครต่อ เก็บข้อมูลไว้ 90 วันแล้วจึงลบ
- สมัครใหม่ด้วยเบอร์เดิมภายใน 90 วัน = กลับเข้าร้านเดิม ไม่ได้ trial ใหม่

---

## 7. สิทธิ์ผู้ใช้งาน

| การกระทำ | owner | admin | staff |
| :--- | :---: | :---: | :---: |
| ดูคิวของตัวเอง | ✅ | ✅ | ✅ |
| ดูคิวทั้งร้าน | ✅ | ✅ | ❌ |
| ยืนยัน/ปฏิเสธสลิป | ✅ | ✅ | ❌ |
| ยกเลิก/เลื่อนคิว | ✅ | ✅ | เฉพาะของตัวเอง |
| แก้บริการและราคา | ✅ | ✅ | ❌ |
| เพิ่ม/ลบพนักงาน | ✅ | ❌ | ❌ |
| แก้ตารางเวลาตัวเอง | ✅ | ✅ | ✅ |
| แก้ตารางเวลาคนอื่น | ✅ | ✅ | ❌ |
| ตั้งค่าร้าน / PromptPay | ✅ | ❌ | ❌ |
| จัดการแพ็กเกจและการชำระเงิน | ✅ | ❌ | ❌ |
| สร้าง ticket | ✅ | ✅ | ✅ |
| ดู/อัปเดตสถานะ ticket | ✅ | ✅ | ✅ |
| ดูตัวอย่าง retention cleanup (ticket) | ✅ | ✅ | ❌ |
| ลบ ticket ที่ปิดแล้ว (retention cleanup) | ✅ | ✅ | ❌ |

ต้องมี helper สามตัวแยกกัน: `is_shop_member()`, `has_shop_role(roles[])`, `is_shop_owner()`

---

## 8. กติกาความปลอดภัยที่ผูกกับกติกาธุรกิจ

1. **Client เขียนตารางหลักโดยตรงไม่ได้เลย** ทุกการสร้าง/เปลี่ยนสถานะ booking ผ่าน database function ที่ควบคุมสิทธิ์เอง
2. **ข้อมูลสาธารณะเปิดผ่าน view ที่กรองคอลัมน์แล้ว** ตาราง `shops` เปิดตรงไม่ได้ เพราะมีเลข PromptPay เบอร์ร้าน และสถานะการสมัครสมาชิกอยู่ในแถวเดียวกัน
3. **ทุก booking ต้องอยู่ร้านเดียวกันทั้งแถว** — `shop_id`, `service_id`, `staff_id`, `customer_id` ต้องเป็นร้านเดียวกัน บังคับด้วย constraint ไม่ใช่พึ่ง RLS
4. **กันจองซ้อนต้องอยู่ที่ชั้น database** ด้วย exclusion constraint บนช่วงเวลาของช่าง ไม่ใช่การเช็คแล้วค่อย insert
5. **Rate limit** การสร้าง hold: 5 ครั้ง/เบอร์/ชั่วโมง และ 30 ครั้ง/IP/ชั่วโมง
6. ราคา ยอดมัดจำ เวลาสิ้นสุด และรหัสจอง คำนวณที่ server ทั้งหมด

ระบบ tickets ใช้แพทเทิร์นเดียวกับข้อ 1 (ไม่มีการเขียนตรงจาก client ทุกอย่างทำงานผ่าน RPC functions: `create_ticket`, `update_ticket_status`, `add_ticket_timeline_entry`, `preview_ticket_retention`, `delete_closed_tickets_before`) เช่นเดียวกับทุกโดเมนในระบบ

---

## 9. การแจ้งเตือน LINE

### 9.1 การผูกตัวตนลูกค้า

```text
https://line.me/R/oaMessage/@{central_oa_id}/?ผูกคิว%20{booking_code}-{link_token}
```

| กติกา | ค่า |
| :--- | :--- |
| ความยาว token | 4 ตัวอักษร ตัดตัวที่สับสน (0/O, 1/I) ออก |
| อายุ token | 24 ชม. หลังสร้างคิว |
| ใช้ได้ | ครั้งเดียว ผูกแล้วเป็นโมฆะ |
| ส่งผิด/หมดอายุ | ตอบข้อความให้ติดต่อร้าน ห้ามบอกว่าคิวนั้นมีอยู่จริงหรือไม่ |

### 9.3 Hybrid Dual-Channel LINE OA Architecture

1. **Default Mode (Central LINE Bot - 0-Friction):**
   - ร้านค้าไม่ต้องกรอก Token หรือสมัคร LINE Developer เอง (0-Friction Onboarding)
   - ระบบส่งข้อความใบนัดอัตโนมัติผ่าน Central LINE Bot (`@central_booking_oa`) โดยระบุชื่อร้านค้าที่บรรทัดแรก
2. **Custom Mode (Custom Shop Messaging API - Advanced):**
   - สำหรับร้านค้าใหญ่ที่ต้องการยิงข้อความออกจาก LINE Official Account ของร้านตนเอง 100%
   - ร้านค้าสามารถกรอก `lineOaId` (แฮนเดิลส่วนตัว) และ `lineChannelToken` ในหน้า Dashboard เพื่อสลับไปยิงตรงผ่าน LINE Developer ของร้านค้า

---

## 10. ระบบหลังบ้านผู้ให้บริการ (Platform Super Admin Panel)

หน้าระบบหลังบ้านกลางสำหรับ CEO (คุณฟรี) ในการบริหารจัดการแพลตฟอร์ม เข้าถึงผ่านเส้นทางเฉพาะ `/platform-admin` (Protected ด้วย Super Admin Role):

1. **Tenant Shop Management:**
   - ตรวจสอบรายชื่อร้านค้าทั้งหมดในระบบ
   - ปรับเปลี่ยนแพ็กเกจบริการ (`free_trial`, `basic_490`, `pro_990`)
   - **ปุ่มยกเลิก/ระงับบริการ (`active` <-> `cancelled` / `suspended`):** สำหรับกดระงับหรือยกเลิกการให้บริการของร้านค้าที่มีปัญหา หรือเปิดคืนบริการเมื่อต่ออายุ
2. **Quota & Usage Ledger:**
   - ติดตามจำนวนคิวจองที่ถูก `confirmed` ในรอบเดือนเทียบกับโควตาของแต่ละแพ็กเกจ
   - **ปุ่มเติมโควตาเสริม Manual (`+100 คิว Topup` = 199 บาท):** สำหรับเติมคิวเสริมให้ร้านค้า
3. **Central LINE OA Gateway & Traffic Monitor:**
   - มอนิเตอร์ยอดข้อความ LINE ที่ยิงออกไปทั้งหมดในเดือนนี้ เทียบกับโควตา LINE Pro 35,000 ข้อความ/เดือน เพื่อควบคุมต้นทุนคงที่ 1,605 บาท/เดือน

---

## 11. สิ่งที่ **ไม่อยู่** ใน V1
- ตรวจสลิปอัตโนมัติ (EasySlip / SlipOK) — อยู่ใน V4
- ตัดเงินสมาชิกอัตโนมัติผ่านบัตร — อยู่ใน V4
- subdomain จริง (ใช้ `/book/{slug}` ไปก่อน)
- หลายสาขา
- แต้มสะสม / สมาชิก
- รายงานเชิงลึก
- ข้อความโต้ตอบสองทางใน LINE (บอทตอบคำถาม)

## 12. การปรับปรุง UI & UX หน้าหลังบ้านร้านค้า (Store Owner Admin Dashboard Polish)

1. **การปรับขนาดตัวหนังสือในตารางจองคิว (+2px ถึง +3px):**
   - เพิ่มขนาดฟอนต์หัวตาราง (`12px text-xs font-bold`), ชื่อลูกค้า (`16px text-base font-bold`), และป้ายสถานะ (`12px text-xs font-semibold`)
2. **ปุ่มยกเลิกคิวงานในตาราง (Row-Level Cancel Booking Button):**
   - เพิ่มปุ่มสีแดง `"ยกเลิกคิว"` ในช่องการจัดการของแต่ละแถว ให้ร้านค้าปรับสถานะเป็น `cancelled` ได้ทันทีเมื่อลูกค้ายกเลิกคิว
3. **ปุ่ม X และปุ่มปิดป๊อบอัพสลิปมัดจำ (Slip Modal Close Controls):**
   - เพิ่มปุ่ม X มุมขวาบน และปุ่ม `"ปิดหน้าต่าง (ยังไม่กดเลือก)"` ด้านล่าง ให้ร้านค้าปิดป๊อบอัพตรวจสลิปได้โดยไม่ต้องบังคับกดอนุมัติหรือปฏิเสธ
4. **การ์ดตารางเวลาพนักงานแบบ 3 คอลัมน์ (Compact 3-Column Square Grid):**
   - ปรับการแสดงผลเวลาเข้า-ออกงานและเวลาพักเที่ยงจากแถบยาวเต็มความกว้าง เป็นการ์ดสี่เหลี่ยม 3 ช่องต่อแถว (`grid grid-cols-1 md:grid-cols-3 gap-4`) ประหยัดพื้นที่สกอร์ลหน้าจอลง 60%
5. **ฟอร์มวันหยุดพิเศษแบบ Split 2-Column Layout:**
   - แบ่งฝั่งซ้าย (ฟอร์มเพิ่มวันหยุดแบบ Vertical Stack) และฝั่งขวา (การ์ดแสดงรายการวันหยุดแบบ Pill Badges มี Scrollbar รองรับวันหยุดเยอะๆ ตลอดปี)
6. **ป้ายกำกับปี ค.ศ. ชัดเจน:**
   - กำชับป้ายเลือกวันที่ด้วย `(ระบุปี ค.ศ. เช่น 2026-08-12)` เพื่อป้องกันร้านค้ากรอกปี พ.ศ.
7. **ภาษาป้ายกำกับ LINE OA ที่เป็นกันเอง:**
   - ปรับใช้คำว่า `"LINE กลางของระบบ (@central_booking_oa)"` แทนคำศัพท์เทคนิค และใช้ป้ายเตือนสีแดง (`text-rose-400 font-medium bg-rose-500/10`) เพื่อความชัดเจนสูงสุด

## 13. กลยุทธ์การโฮสติ้งและ Deploy ระบบ (Deployment Architecture & Cloud Hosting Strategy)

1. **การมุ่งสู่ Cloudflare Pages (Commercial-Allowed Free Deployment):**
   - ในช่วงพัฒนา ทดสอบ และเปิดตัวเริ่มต้น (No Custom Domain Yet) ระบบจะมุ่ง Deploy บน **Cloudflare Pages** เพื่อใช้ประโยชน์จากสิทธิ์การใช้งานเชิงพาณิชย์ฟรี (Commercial Use Allowed 100%) และไม่จำกัด Bandwidth
2. **โครงสร้าง URL ฟรีชั่วคราวช่วงทดสอบ (.pages.dev):**
   - **หน้าจองฝั่งลูกค้า (`apps/booking-consumer`):** `https://local-booking-customer.pages.dev/book/good-cuts-barber`
   - **หน้าหลังบ้านร้านค้า + CEO Panel (`apps/booking-admin`):** `https://local-booking-admin.pages.dev/dashboard` และ `/platform-admin`
3. **การต่อยอดสู่ Custom Domain (Production Launch):**
   - เมื่อพร้อมเปิดตัวทางการ ค่อยจดโดเมนหลัก 1 ชื่อ (เช่น `queueeasy.com` ~350-450 บาท/ปี) แล้วผูกผ่าน Cloudflare DNS 1-Click โดยอัตโนมัติ

## 14. ระบบสมัครสมาชิกร้านค้าใหม่ (Shop Owner Onboarding & Registration `/register`)

1. **4-Step Wizard Flow:**
   - **Step 1:** ข้อมูลร้านค้าและเจ้าของร้าน (Auto-Slug Generator + Free-text Business Category input พร้อม Quick Suggestion Chips)
   - **Step 2:** เลือกแพ็กเกจ (Free Trial 14 วัน / Basic ฿490 / Pro ฿990) พร้อมสวิตช์รายเดือน/รายปี (`ประหยัด 2 เดือน`)
   - **Step 3:** ตั้งค่า PromptPay รับเงินมัดจำ (เลขพร้อมเพย์ & ชื่อบัญชี)
   - **Step 4:** สร้างร้านค้าสำเร็จ วาร์ปเข้าแดชบอร์ดทันที
2. **Dynamic URL Pre-selection:**
   - รองรับ Query Parameter เช่น `/register?plan=basic_490` หรือ `/register?plan=pro_990` สำหรับกดเลือกโปรล่วงหน้ามาจากแดชบอร์ด

---

## 15. ศูนย์ควบคุมหลังบ้านผู้ให้บริการ (Platform Super Admin & Pending Approval Queue `/platform-admin`)

1. **คิวอนุมัติร้านค้าใหม่ (Pending Shop Approval Queue):**
   - แสดงการ์ดการแจ้งเตือนสีส้มเด่นชัดเมื่อมีร้านค้าสมัครใหม่เข้ามาในระบบ เพื่อให้ CEO (คุณฟรี) กดอนุมัติเปิดใช้งาน (`Approve & Activate`), ปฏิเสธ (`Reject`), หรือโทรติดต่อเจ้าของร้านได้ทันที
2. **ระบบสลับดูตามสถานะร้านค้า (Status Filter Tabs):**
   - **ร้านค้าใช้งานอยู่ (Active):** แสดงเฉพาะร้านที่เปิดให้บริการ ซ่อนร้านที่ยกเลิก/ระงับบริการโดยอัตโนมัติ เพื่อรักษาความสะอาดของหน้าแดชบอร์ด
   - **รออนุมัติ (Pending Approval):** คิวร้านค้าใหม่ที่รอ CEO กดอนุมัติ
   - **ยกเลิก/ระงับบริการ (Cancelled/Suspended):** แท็บเฉพาะสำหรับดูและกดเปิดคืนบริการร้านค้าที่ถูกระงับ
3. **ปุ่มแอบดูหลังบ้านร้านค้า (`👁️ ดูหลังบ้าน`):**
   - ปุ่มทางลัดให้ CEO วาร์ปเข้าไปดูและตรวจสอบแดชบอร์ดหลังบ้านของร้านค้านั้นๆ เพื่อช่วยเหลือแก้ไขปัญหาได้ทันที
4. **ปุ่มขยายเวลาแพ็กเกจ manual (`+14 วัน`):**
   - ปุ่มทางลัดให้ CEO กดแถมหรือขยายเวลาใช้งานให้ร้านค้าเพิ่ม 14 วันโดยอัตโนมัติ
5. **คอลัมน์วันหมดอายุ/ตัดรอบบิลถัดไป (Expiration & Renewal Date Column):**
   - แสดงวันที่สิ้นสุด Free Trial หรือวันตัดรอบบิลชำระเงินถัดไปอย่างชัดเจนในตาราง

---

## 16. การผ่านการตรวจวิเคราะห์คุณภาพและแก้ไขบั๊กวิกฤต (Independent Audit & Bug Resolution)

1. **ระบบ Slug ลิงก์ร้านค้าภาษาไทย & URL Editable Input (`/register`):**
   - แก้ไขปัญหาร้านค้าชื่อไทยโดนลบตัวอักษรกลายเป็น `my-shop` ซ้ำกัน โดยเปลี่ยนมาใช้การเจน unique slug `shop-XXX` พร้อมเปิดช่องให้เจ้าของร้านแก้ไขสลัก URL ภาษาอังกฤษได้ตามต้องการ
   - เพิ่มข้อความกำกับกระชับ: `💡 หมายเหตุ: URL ใช้ภาษาอังกฤษเพื่อป้องกันการแปลงภาษาผิด`
2. **ระบบล็อกสล็อตเวลา 15 นาทีแท้จริง (15-Min Hold Timer Expiration Lock):**
   - เมื่อเวลานับถอยหลังหมดลง (`00:00`) ระบบจะเปลี่ยนแถบแจ้งเตือนเป็นสีแดงบล็อกการยืนยันจองทันที พร้อมเพิ่มปุ่ม `🔄 เลือกรอบเวลาใหม่` ให้กดกลับไปเลือกเวลาได้สะอาดตา 1 คลิก
3. **ฟังก์ชันดาวน์โหลดรูป PromptPay QR มัดจำ (.png):**
   - ปุ่ม `บันทึกรูป QR Code` ทำการดาวน์โหลดไฟล์ภาพ QR Code มัดจำลงมือถือ/คอมพิวเตอร์ของลูกค้าจริง
4. **หน้ากู้คืนรหัสผ่านร้านค้า (`/forgot-password`):**
   - หน้าจอกู้คืนรหัสผ่านร้านค้าสำหรับรองรับการส่งอีเมลรีเซ็ตรหัสผ่านใน Phase 1

---

## 17. บัคที่ทราบแล้ว รอแก้ตอนต่อ Backend จริง (Known Issues — Pre-Backend-Integration)

> ตรวจพบระหว่างการรีวิวภาพรวมโปรเจค 2026-08-06 ก่อนเริ่มเฟสต่อหลังบ้าน (Supabase/Stripe/LINE) — บันทึกไว้กันลืมและกันสับสนว่า "เสร็จแล้ว" ทั้งที่ยังเป็น mockup

1. **Frontend ยังไม่เชื่อม Backend จริงทั้งระบบ:** ทุกหน้า (`/register`, `/dashboard`, `/book/[slug]`) ใช้ `useState` เก็บข้อมูลใน browser เท่านั้น ไม่มีหน้าไหนเรียก Supabase client (`lib/supabase.ts` มีอยู่แต่ไม่ถูก import ใช้งานจริงเลยสักไฟล์) แม้ DB schema/RLS/RPC functions จะเขียนรองรับไว้ครบใน `supabase/migrations/` แล้วก็ตาม
2. **Stripe ยังไม่ได้ติดตั้ง:** ไม่มี `stripe` package ใน dependencies และไม่มี checkout/webhook route ใดๆ — คำว่า "Stripe" ที่ปรากฏใน `dashboard/page.tsx` เป็นแค่ข้อความ UI
3. **LINE Webhook รับ event ได้แต่ไม่ประมวลผลจริง:** `apps/*/src/app/api/line/webhook/route.ts` แค่ `console.log` ข้อความที่เข้ามา ไม่ได้ยิงข้อความกลับหรือผูก customer identity ตามที่ระบุในข้อ 9.1
4. **Supabase project ใน README (`gyleqrjdzwwlqierdwcy.supabase.co`) หาไม่พบในบัญชีที่ต่อ MCP ปัจจุบัน** ต้องตรวจสอบว่ามีอยู่จริงหรือไม่ก่อนเริ่มต่อหลังบ้าน — ไม่มีไฟล์ `.env.local` ในทั้งสอง apps ด้วย
5. **Slug generator สุ่มค่าใหม่ทุกครั้งที่พิมพ์ (`register/page.tsx`):** ถ้าชื่อร้านเป็นภาษาไทยล้วน ทุกตัวอักษรที่พิมพ์จะเรียก `Math.random()` ใหม่ ทำให้ slug กระพริบเปลี่ยนค่าทุก keystroke แทนที่จะ generate ครั้งเดียว
6. **ช่อง Password ไม่บังคับความยาวจริง:** placeholder บอก "อย่างน้อย 8 ตัวอักษร" แต่ไม่มี `minLength={8}` กรอก 1 ตัวอักษรก็ผ่านฟอร์มได้
7. **อัปโหลดสลิปไม่ validate ขนาด/ประเภทไฟล์ฝั่ง client:** ตามข้อ 4.3 ต้องจำกัด jpg/png/pdf ≤5MB แต่โค้ดปัจจุบันพึ่ง `accept="image/*"` ของ browser เท่านั้น ซึ่งเลี่ยงได้ง่าย

---

## 📌 เอกสารที่เกี่ยวข้องในระบบ (Document Map)
- 🏠 **ดัชนีภาพรวมระบบ:** [`README.md`](file:///D:/AI-Workspace/projects/local-service-booking-saas/README.md)
- 📄 **บรีฟสเปกทางเทคนิค Phase 1:** [`docs/technical/BRIEF_PHASE1_AGY.md`](file:///D:/AI-Workspace/projects/local-service-booking-saas/docs/technical/BRIEF_PHASE1_AGY.md)
- 🛡️ **มาตรฐานความปลอดภัย:** [`docs/technical/ARCHITECTURE_SECURITY_STANDARD.md`](file:///D:/AI-Workspace/projects/local-service-booking-saas/docs/technical/ARCHITECTURE_SECURITY_STANDARD.md)
- 🏆 **แผนธุรกิจ & การเงิน:** [`docs/business/OFFICIAL_BUSINESS_MODEL.md`](file:///D:/AI-Workspace/projects/local-service-booking-saas/docs/business/OFFICIAL_BUSINESS_MODEL.md)
- 💰 **ข้อกำหนดราคาแพ็กเกจ:** [`docs/business/PRICING_SPEC.md`](file:///D:/AI-Workspace/projects/local-service-booking-saas/docs/business/PRICING_SPEC.md)
