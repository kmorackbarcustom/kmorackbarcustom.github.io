'use client';

import { useParams } from 'next/navigation';
import { AlertTriangle, PackageOpen } from 'lucide-react';
import { getProductionOrderRuntimeStatus } from '@/lib/order-runtime';

export default function OrderScaffoldPage() {
  const params = useParams();
  const slug = typeof params?.slug === 'string' ? params.slug : 'shop';
  const runtime = getProductionOrderRuntimeStatus();
  const steps = ['สินค้า', 'จำนวน', 'วันที่พร้อมรับ', 'ข้อมูลลูกค้า', 'การรับสินค้า', 'สรุปคำสั่งซื้อ'];
  return <main data-runtime-status={runtime.code} className="min-h-screen bg-slate-950 px-4 py-10 text-slate-100"><section className="mx-auto max-w-3xl space-y-5"><header className="rounded-2xl border border-slate-800 bg-slate-900 p-6"><div className="flex items-center gap-3"><PackageOpen className="h-9 w-9 text-emerald-400" /><div><p className="text-xs text-emerald-300">Order V1 · {slug}</p><h1 className="text-xl font-bold">สั่งสินค้ากับร้านนี้</h1></div></div><p className="mt-3 text-sm text-slate-300">โครงหน้าสั่งซื้อพร้อมเชื่อม runtime ภายหลัง โดยตอนนี้ยังไม่เปิดรับคำสั่งซื้อจริง</p></header><nav aria-label="Order steps" className="grid grid-cols-2 gap-2 sm:grid-cols-6">{steps.map((step, index) => <div key={step} className="rounded-lg border border-slate-800 bg-slate-900/70 p-2 text-center text-[11px] text-slate-400"><span className="mr-1 text-emerald-400">{index + 1}</span>{step}</div>)}</nav><div className="grid gap-5 md:grid-cols-2"><section className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><h2 className="font-semibold">Catalog และจำนวน</h2><div className="mt-3 rounded-xl border border-dashed border-slate-700 p-4 text-sm text-slate-400">รายการสินค้าและ quantity selector จะโหลดจาก catalog repository หลัง runtime พร้อม</div><button disabled className="mt-4 w-full rounded-xl bg-slate-800 px-4 py-3 text-xs font-semibold text-slate-500">เลือกสินค้า (ยังไม่พร้อม)</button></section><section className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><h2 className="font-semibold">Ready-date และ fulfillment</h2><div className="mt-3 space-y-2 text-sm text-slate-400"><p>วันที่พร้อมรับ: unavailable</p><p>ตัวเลือก: PICKUP · DELIVERY · ON_SITE_SERVICE</p><p>นัดหมาย Booking: สร้างได้เมื่อ Order เป็น READY เท่านั้น</p></div></section><section className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><h2 className="font-semibold">ข้อมูลลูกค้า</h2><fieldset disabled className="mt-3 grid gap-2"><label className="text-xs text-slate-400">ชื่อลูกค้า<input className="mt-1 w-full rounded-xl border border-slate-800 bg-slate-950 px-3 py-2 text-sm" /></label><label className="text-xs text-slate-400">เบอร์โทรศัพท์<input className="mt-1 w-full rounded-xl border border-slate-800 bg-slate-950 px-3 py-2 text-sm" /></label></fieldset></section><section className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><h2 className="font-semibold">สรุปและติดตามคำสั่งซื้อ</h2><p className="mt-3 text-sm text-slate-400">ระบบจะใช้ immutable line snapshot และ opaque tracking token เมื่อ runtime ผ่าน gate</p><label className="mt-3 block text-xs text-slate-400">Tracking token<input disabled className="mt-1 w-full rounded-xl border border-slate-800 bg-slate-950 px-3 py-2 text-sm" /></label><button disabled className="mt-3 w-full rounded-xl bg-slate-800 px-4 py-3 text-xs font-semibold text-slate-500">ติดตามคำสั่งซื้อ (ปิดชั่วคราว)</button><button disabled className="mt-2 w-full rounded-xl bg-emerald-500/20 px-4 py-3 text-xs font-semibold text-emerald-300">ส่งคำสั่งซื้อ (ปิดชั่วคราว)</button></section></div><div className="flex items-center justify-center gap-2 rounded-xl border border-amber-500/30 bg-amber-500/10 p-3 text-xs text-amber-200"><AlertTriangle className="h-4 w-4" />ยังไม่เปิดรับคำสั่งซื้อ — ไม่มีการสร้างรายการ ชำระเงิน จองกำลังผลิต หรือ tracking สำเร็จปลอม</div></section></main>;
}
