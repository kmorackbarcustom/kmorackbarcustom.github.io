import { AlertTriangle, ClipboardList } from 'lucide-react';

export default function OrdersPage() {
  return <main className="space-y-6 p-6 text-slate-100"><header><div className="flex items-center gap-3"><ClipboardList className="h-7 w-7 text-emerald-400" /><div><p className="text-xs text-emerald-300">Order V1</p><h1 className="text-2xl font-bold">คำสั่งซื้อ</h1></div></div><p className="mt-2 text-sm text-slate-400">โครงรายการและ action boundaries สำหรับเชื่อม runtime หลัง shared-runtime gate</p></header><div className="grid gap-4 md:grid-cols-4">{['DRAFT', 'SUBMITTED', 'CONFIRMED', 'READY'].map((state) => <div key={state} className="rounded-xl border border-slate-800 bg-slate-900 p-4"><p className="text-xs text-slate-400">{state}</p><p className="mt-2 text-2xl font-bold text-slate-500">—</p></div>)}</div><section className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><h2 className="font-semibold">รายการ Order</h2><div className="mt-3 rounded-xl border border-dashed border-slate-700 p-6 text-center text-sm text-slate-400">ยังไม่มี live Order repository — ไม่แสดงข้อมูลปลอมหรือสร้างคำสั่งซื้อจากหน้านี้</div><div className="mt-5 flex flex-wrap gap-2">{['Confirm / Reject', 'Start production', 'Mark READY', 'Complete', 'Ready-date override + audit', 'Payment / deposit review'].map((action) => <button key={action} disabled className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-2 text-xs text-slate-600">{action}</button>)}</div></section><div className="flex items-center gap-2 rounded-xl border border-amber-500/30 bg-amber-500/10 p-3 text-xs text-amber-200"><AlertTriangle className="h-4 w-4" />Order runtime ยังไม่เปิด — action ทั้งหมดปิดเพื่อป้องกัน fake state</div></main>;
}
